# Backend Environment Variables
#
# SQLALCHEMY_DATABASE_URL: Database connection string.
# Example for SQLite: sqlite:///./sql_app.db
# Example for PostgreSQL: postgresql://user:password@host:port/database_name
SQLALCHEMY_DATABASE_URL="sqlite:///./sql_app.db"

# SECRET_KEY: A strong, unique secret key for JWT token encoding.
# You can generate one using: openssl rand -hex 32
SECRET_KEY="your_super_secret_key_here"

# ALGORITHM: Algorithm for JWT token encoding (default: HS256)
ALGORITHM="HS256"

# ACCESS_TOKEN_EXPIRE_MINUTES: Access token expiration time in minutes (default: 60)
ACCESS_TOKEN_EXPIRE_MINUTES=60

# FIRST_SUPERUSER: Username for the first superuser account.
FIRST_SUPERUSER="admin"

# FIRST_SUPERUSER_PASSWORD: Password for the first superuser account.
FIRST_SUPERUSER_PASSWORD="admin_password"

# Email settings for password recovery and notifications.
# If not configured, email functionality will be limited (e.g., links printed to console).
MAIL_USERNAME=""
MAIL_PASSWORD=""
MAIL_FROM=""
MAIL_PORT=587
MAIL_SERVER=""
MAIL_TLS=True
MAIL_SSL=False

# DEBUG: Set to True for development, False for production.
DEBUG=True

# UPLOAD_DIR: Directory to store uploaded files (default: uploads)
UPLOAD_DIR="uploads"

# API_BASE_URL: Base URL for the backend API.
# This might be used by the frontend to construct API requests.
# For local development with the frontend proxy, this might not be directly used by the frontend,
# but it's good to define it for backend-initiated links (e.g., password reset emails).
API_BASE_URL="http://localhost:8000"

# BACKEND_CORS_ORIGINS: Comma-separated list of origins that are allowed to make cross-origin requests.
# Example: http://localhost:3000,http://127.0.0.1:3000
BACKEND_CORS_ORIGINS="http://localhost:3000,http://127.0.0.1:3000"

# PROJECT_NAME: Name of the project (default: Chack)
PROJECT_NAME="Chack"

# PROJECT_VERSION: Version of the project (default: 1.0.0)
PROJECT_VERSION="1.0.0"

# API_V1_STR: Base path for API version 1 (default: /api/v1)
API_V1_STR="/api/v1"

# Frontend Environment Variables (if any, as derived from nuxt.config.ts)
# The frontend primarily uses a proxy for API calls during development,
# so explicit API_BASE_URL might not be needed in a frontend .env file,
# but it's included here for completeness if you decide to not use a proxy.

# NODE_ENV: Set to 'production' for production builds to disable devtools and drop console/debugger statements.
NODE_ENV="development"
